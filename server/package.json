{
  "name": "server",
  "version": "1.0.0",
  "description": "AIRLOCK local control-plane API",
  "main": "index.js",
  "scripts": {
    "postinstall": "npm --prefix ../contracts ci && npm --prefix ../contracts run build",
    "start": "node --env-file-if-exists=.env index.js",
    "test": "node --test"
  },
  "keywords": [],
  "author": "",
  "license": "MIT",
  "type": "commonjs",
  "dependencies": {
    "ethers": "^6.17.0",
    "sigstore": "5.0.0"
  }
}
